export type DialogsTypes = {
    dialogs: {
        name: string
        time: string
        shortMessage: string
        amountMessages: number
        avatar?: string
        active?: boolean
    }[]
}
